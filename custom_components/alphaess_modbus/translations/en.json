{
  "config": {
    "step": {
      "user": {
        "title": "Set up AlphaESS Modbus",
        "description": "Configure connection to your AlphaESS inverter",
        "data": {
          "host": "Host / IP address",
          "port": "Port",
          "slave_id": "Slave ID",
          "model": "Inverter model",
          "poll_freq": "Polling frequency",
          "ac_limit_kw": "Inverter AC limit (kW)"
        },
        "data_description": {
          "poll_freq": "Fast = 1s (high load), Medium = 5s (default), Slow = 10s (recommended for RS485 converters)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to inverter",
      "invalid_connection": "Please enter a valid host / IP address",
      "invalid_model": "Please select an inverter model",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AlphaESS Modbus options",
        "data": {
          "host": "Host / IP address",
          "port": "Port",
          "slave_id": "Slave ID",
          "model": "Inverter model",
          "poll_freq": "Polling frequency",
          "ac_limit_kw": "Inverter AC limit (kW)"
        },
        "data_description": {
          "poll_freq": "Fast = 1s (high load), Medium = 5s (default), Slow = 10s (recommended for RS485 converters)"
        }
      }
    }
  },
  "selector": {
    "poll_freq": {
      "options": {
        "slow": "Slow",
        "medium": "Medium",
        "fast": "Fast"
      }
    }
  }
}
